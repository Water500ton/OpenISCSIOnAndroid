# Open-iSCSI for Android

基于 [open-iscsi](https://github.com/open-iscsi/open-iscsi) `2.1.13` 移植的
ARM64 Android 用户态 iSCSI 发起端，打包为 **KernelSU / Magisk 模块**。

项目只包含用户态控制面（`iscsid`、`iscsiadm` 等）。iSCSI 内核数据面
（`scsi_transport_iscsi`、`libiscsi`、`iscsi_tcp`）由设备内核提供。

## 功能

- 静态交叉编译的 ARM64 Open-iSCSI 用户态工具
- 去掉 Android 不存在的依赖：`libkmod`、`libmount`、`systemd`、`open-isns`
- CHAP 认证使用静态编译的 OpenSSL `libcrypto`
- 所有配置和运行数据统一放在 `/data/iscsi`
- 模块安装后自动生成初始配置和 InitiatorName
- 支持 `iscsiadm` 发现、登录、会话管理和注销

## 已编译产物

| 文件 | 用途 |
| --- | --- |
| `iscsid` | iSCSI 守护进程 |
| `iscsiadm` | iSCSI 管理工具 |
| `iscsistart` | 启动时登录工具 |
| `iscsi-iname` | 生成 InitiatorName |
| `libopeniscsiusr.so.0.2.0` | 运行时共享库 |

最低系统要求：**Android 8.0（API 26）**，**ARM64**。

## 下载与安装

从 Release 页面下载最新模块包：

- Release 页面：https://github.com/Water500ton/OpenISCSIOnAndroid/releases
- 模块文件：`OpenISCSIOnAndroid-KernelSU-arm64-v2.1.13.zip`

安装步骤：

1. 在 KernelSU 或 Magisk Manager 中选择该 zip 安装。
2. 重启设备。
3. 首次启动会自动创建 `/data/iscsi`，并复制 `iscsid.conf`、生成
   `initiatorname.iscsi`。
4. `service.sh` 会自动启动 `iscsid`。

## SELinux 要求

当前版本需要将 SELinux 设置为**宽容模式（Permissive）**，否则 `iscsid`
可能无法访问必要的内核接口或在 `/data/iscsi` 下创建文件。

在已获得 root 权限的设备上执行：

```sh
su -c 'setenforce 0'
```

查看当前状态：

```sh
su -c 'getenforce'
```

注意：`setenforce 0` 重启后不会保留。若希望持久生效，请在
KernelSU/Magisk 的启动脚本中调用上述命令，或根据你的设备配置合适的
sepolicy 规则。

## 配置与运行目录

编译进程序的路径均为 `/data/iscsi`：

```text
/data/iscsi
├── iscsid.conf           # 主配置文件
├── initiatorname.iscsi   # InitiatorName
├── iscsid.pid            # 守护进程 PID
├── iscsid.log            # 守护进程日志
├── ifaces/               # 接口配置
├── nodes/                # 节点数据库
├── send_targets/         # SendTargets 发现记录
├── static/
├── isns/
└── fw/
```

## 使用示例

确保 `iscsid` 已运行，然后以 root 执行：

```sh
# 发现目标
su -c '/system/bin/iscsiadm -m discovery -t sendtargets -p 192.168.1.10:3260'

# 登录所有发现的节点
su -c '/system/bin/iscsiadm -m node --login'

# 查看会话
su -c '/system/bin/iscsiadm -m session'

# 注销会话
su -c '/system/bin/iscsiadm -m node --logout'
```

## 从源码构建

依赖：

- Android NDK r27 或更高版本
- `curl`、`tar`、`make`、`python3`

执行：

```sh
ANDROID_NDK_HOME=/path/to/android-ndk ./android/build.sh
```

脚本会：

1. 下载并静态编译 OpenSSL 3.0.15。
2. 交叉编译 `libopeniscsiusr`、`iscsid`、`iscsiadm`、`iscsistart` 和
   `iscsi-iname`。
3. 将产物放入 `module/system/`。
4. 打包生成 `OpenISCSIOnAndroid-KernelSU-arm64-v2.1.13.zip`。

## 仓库结构

```text
.
├── android/                  # Android 构建脚本
│   └── build.sh
├── module/                   # KernelSU 模块内容
│   ├── module.prop
│   ├── customize.sh
│   ├── post-fs-data.sh
│   ├── service.sh
│   ├── uninstall.sh
│   ├── config/iscsid.conf
│   └── system/
│       ├── bin/              # 编译后的 ARM64 二进制
│       └── lib64/            # libopeniscsiusr
├── usr/                      # open-iscsi 用户态源码（含 Android 适配）
├── libopeniscsiusr/          # 用户态库源码（含 Android 适配）
└── .github/workflows/        # 自动构建 Release 的 CI
    └── release.yml
```

## 内核模块

用户态工具默认认为设备内核已包含 `scsi_transport_iscsi`、`libiscsi` 和
`iscsi_tcp`。如果你的内核将这些功能编译为独立模块，请把 `iscsi_tcp.ko`
放入模块覆盖目录，例如：

```text
module/system/lib64/modules/iscsi_tcp.ko
```

Android 适配层会通过 `finit_module(2)` 探测常见模块目录，包括
`/system/lib64/modules`、`/vendor/lib64/modules` 以及当前
KernelSU/Magisk 模块目录。

## 故障排查

查看守护进程日志：

```sh
su -c 'cat /data/iscsi/iscsid.log'
```

确认守护进程是否运行：

```sh
su -c 'ps -A | grep iscsid'
```

确认 SELinux 状态：

```sh
su -c 'getenforce'
```

若返回 `Enforcing`，请先切换到宽容模式：

```sh
su -c 'setenforce 0'
```

## 卸载

在 KernelSU/Magisk Manager 中移除模块即可。卸载脚本会停止 `iscsid`，
但会保留 `/data/iscsi` 中的配置和节点数据，便于以后重新安装时继续使用。

## 许可证

- `iscsid`、`iscsiadm`、`iscsistart` 等命令：GPLv3
- `libopeniscsiusr`：LGPLv3
- 原始项目：https://github.com/open-iscsi/open-iscsi
